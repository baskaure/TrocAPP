/**
 * Constantes de mise en page (header fixe + sidebar) pour aligner toutes les vues.
 * Référence visuelle : espacement sous le header comme sur la page « Annonces » (titre L'échange…).
 */
export const APP_HEADER_HEIGHT_CLASS = 'h-20';

/** Padding-top du <main> : même décalage que la page Annonces (titre « L'échange… ») sous le header fixe h-20 */
export const APP_MAIN_PADDING_TOP_CLASS = 'pt-28';

/** Décalage à gauche quand la sidebar est visible : largeur sidebar (16rem) + écart contenu (2rem, aligné sur px-8) */
export const APP_SIDEBAR_CONTENT_INSET_LG = 'lg:pl-[calc(16rem+2rem)]';
