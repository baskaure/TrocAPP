[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--no-audit --no-fund"

# Les redirections SPA et les en-têtes HTTP vivent dans public/_redirects et public/_headers
