# Variables front (exposées au navigateur — la clé anon est publique par conception)
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon public key>

# Variables serveur : à définir dans Supabase → Edge Functions → Secrets (jamais dans ce fichier)
# RESEND_API_KEY=re_...
# EMAIL_FROM="BonTroc <noreply@bontroc.fr>"
